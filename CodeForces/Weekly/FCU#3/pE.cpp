#include <bits/stdc++.h>
using namespace std;
#define chen ios::sync_with_stdio(0);cin.tie(0)
int main(){
    //chen;
    int n,l,r;
    cin>>n>>l>>r;
    vector<int> arr(n);
    for(int i = 0 ; i < n ; i++){
        cin>>arr[i];
    }
    for(int i = l ; i <= r ; i++){
        for(int j = i ; j < n ; j++){
            
        }
    }
    system("pause");
    return 0;
}